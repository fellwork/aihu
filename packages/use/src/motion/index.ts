/**
 * `@aihu/use/motion` — family aggregate barrel (namespace-wave0).
 * Re-exports every motion member for the bare `@aihu/use/motion` entry.
 */
export type { UseReducedMotionReturn } from './useReducedMotion/index.ts'
export { useReducedMotion } from './useReducedMotion/index.ts'
