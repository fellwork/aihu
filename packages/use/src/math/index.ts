/**
 * `@aihu/use/math` — family aggregate barrel (namespace-wave0).
 * Re-exports every math member for the bare `@aihu/use/math` entry.
 */
export type { UseClampReturn } from './useClamp/index.ts'
export { useClamp } from './useClamp/index.ts'
