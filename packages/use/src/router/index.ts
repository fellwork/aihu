/**
 * `@aihu/use/router` — family aggregate barrel (namespace-wave0).
 * Re-exports every router member for the bare `@aihu/use/router` entry.
 * Every member in this family consents to the `@aihu/router` +
 * `@aihu/context` peers declared in `families.json`'s `router["*"]` entry.
 */
export type { UseRouteParamsReturn } from './useRouteParams/index.ts'
export { useRouteParams } from './useRouteParams/index.ts'
