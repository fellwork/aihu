/**
 * `@aihu/primitives/button` — headless button base. Re-exports the
 * `AihuButton` base class + the `defineButton(tag)` registration helper.
 */
export { AihuButton, type ButtonType, defineButton } from './button.ts'
