@state {
  import { signal } from '@aihu/signals'
  const [count, setCount] = signal(0)
  const [label, setLabel] = signal('count')
}

@template {
  <div class="counter">
    <input $bind:value={label} placeholder="label" />
    <button $on:click={() => setCount(count() - 1)}>-</button>
    <span>{label()}: {count()}</span>
    <button $on:click={() => setCount(count() + 1)}>+</button>
  </div>
}

@style {
  .counter { display: flex; gap: 1rem; align-items: center; font-size: 1.5rem; }
  input { padding: 0.25rem; font-size: 1rem; border: 1px solid #ccc; border-radius: 4px; }
  button { padding: 0.25rem 0.75rem; cursor: pointer; }
}
