{
  "tools": [
    {
      "name": "airtime_quote",
      "tag": "airtime-quote",
      "inputs": {
        "plan": { "type": "enum", "values": ["daily", "weekly", "monthly"], "default": "daily" },
        "amount": { "type": "number", "default": "100" }
      },
      "actions": {
        "quote": {
          "returns": {
            "plan": { "type": "string" },
            "amount": { "type": "number" },
            "fee": { "type": "number" },
            "total": { "type": "number" }
          }
        }
      }
    }
  ]
}
