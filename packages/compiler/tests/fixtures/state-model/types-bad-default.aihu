// #487 §5.3 — a wrong-typed `default:` is a tsc error ON THE AUTHORED LINE:
// the config object is a real TS expression checked against PropConfig<T>.

@state {
  const count = prop<number>({ default: 'not-a-number' })
}

@template {
  <p>{count}</p>
}
