// #487 §5.2 — a defaultless, non-required prop is `T | undefined`; unguarded
// member access on it must FAIL `tsc --strict` (TS18048).

@state {
  const nickname = prop<string>()
}

@template {
  <p>{nickname.length}</p>
}
