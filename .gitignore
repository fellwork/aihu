# Dependencies
node_modules/
.pnpm-store/
pnpm-debug.log*

# Rust / Cargo
target/
**/*.rs.bk
Cargo.lock.bak

# Build output
dist/
build/
*.tsbuildinfo
examples/*/dist/
# Compiler-generated TypeScript sidecar files (type-info artifacts from rolldown plugin)
**/*.aihu.ts

# Vite
.vite/

# Rolldown cache files (hashed temp artifacts from build)
rolldown.config.*.js

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.idea/
.vscode/
*.swp
*.swo
*~

# Claude Code local settings (machine-specific)
.claude/settings.local.json
# Claude Code subagent worktrees (transient agent workspaces)
.claude/worktrees/
# Top-level worktrees (transient feature-branch worktrees)
.worktrees/

# Test artifacts
coverage/
.nyc_output/
playwright-report/
test-results/

# moon
.moon/cache
.moon/docker

# AGENTS.db local layer (session scratch, gitignored per fw-agent-skill middleware)
AGENTS.local.db

# Bench memory pass intermediate file (folded into RESULTS.md by runner)
bench/signals/RESULTS.memory.json
bench/arbor/RESULTS.memory.json

# Lighthouse CI results (generated by scripts/lighthouse.ts)
scripts/lighthouse-results.json
.gstack/

# Docs build output (generated by scripts/build-docs.ts)
docs/site/dist/

# VS Code extension build artifacts
*.vsix

# WASM build artifacts (Directive 1 + arch-4 §4.6)
packages/compiler/pkg-wasm/

# Agent team session artifacts — director notes, retros, build manifests, state files
# (consolidate ratified specs into docs/specs/ and plans into docs/plans/ instead)
.team/
state-*.md
docs/superpowers/
docs/topic-director-notes/
docs/topic-summaries/
docs/build-manifest-*.md
docs/investigation-*.md
docs/retro-*.md
docs/tthw-log.md
docs/spec-headless.md

# Stray session log files
[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]-*.txt
