---
"create-aihu": minor
---

Add the `create-aihu` entry-point package so `npm create aihu` / `npx create-aihu` / `bun create aihu` resolve on npm (previously 404). Thin delegator to @aihu/cli's scaffolder.
