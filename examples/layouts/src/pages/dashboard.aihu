@route {
  path: "/dashboard",
  name: "dashboard-page",
  layout: "compact"
}

@template {
  <section class="dashboard-page">
    <h1>Dashboard</h1>
    <p>This page declares <code>layout: "compact"</code>, so navigating here from
       Home swaps the whole shell — proving the layout is resolved per route at
       runtime.</p>
    <p>Back to <a href="/">Home</a>.</p>
  </section>
}

@style {
  /* DA4: light-DOM pages share the global cascade — the root class must be
     UNIQUE per page (index.aihu had the same `.page` scope with different
     values; last-loaded silently won). */
  .dashboard-page h1 { font-size: 1.4rem; margin-bottom: 0.5rem; }
  .dashboard-page p { line-height: 1.55; margin-bottom: 0.6rem; color: #334155; font-size: 0.95rem; }
  .dashboard-page a { color: #2563eb; }
  .dashboard-page code { background: #f1f5f9; padding: 0.05rem 0.3rem; border-radius: 3px; font-size: 0.9em; }
}
