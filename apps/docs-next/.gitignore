dist/
node_modules/
src/styles/utilities.generated.css
test-results/
# Playground build artifacts staged into public/ by `prebuild` (see
# scripts/stage-wasm.ts + rolldown.preview.config.ts). Rebuilt from the
# checkout every build so the playground grammar matches this commit (#491);
# the .wasm alone is ~1 MB and must never be committed.
public/wasm/
public/aihu-preview-bundle.js
# .design-review/ is committed intentionally — the founder's design-review renders.
