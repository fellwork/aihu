// GENERATED by scripts/gen-api.ts — do not edit. Source: packages/plugin-agent-readiness
import type { ApiExport, ApiPackage } from '../types.ts'

export const PKG: ApiPackage = {
  name: '@aihu-plugin/agent-readiness',
  slug: 'plugin-agent-readiness',
  tier: 'Plugins',
  version: '2.2.2',
  tagline: 'Discovery + readiness manifest emitter so agents can introspect aihu apps.',
  note: '',
}

export const EXPORTS: readonly ApiExport[] = [
  {
    name: 'classifyBotUserAgent',
    kind: 'function',
    signature: 'function classifyBotUserAgent(userAgent: string): BotTier | null',
    summary: 'Classify a User-Agent string against the unified bot registry.',
    agent: true,
  },
  {
    name: 'componentsFromManifestJson',
    kind: 'function',
    signature: 'function componentsFromManifestJson(text: string): ReadonlyArray<AgentMetadata>',
    summary: "Parse one sidecar's JSON text into registry metadata.",
  },
  {
    name: 'createAgentReadinessRoutes',
    kind: 'function',
    signature:
      'function createAgentReadinessRoutes( config: AgentReadinessConfig, sources: AgentReadinessSources = {}, ): { readonly llmsTxt: RouteHandler readonly llmsFullTxt: RouteHandler readonly mcpServerCard: RouteHandler readonly robotsTxt: RouteHandler readonly a2aCard: RouteHandler readonly mcpDiscovery: RouteHandler readonly sitemapXml: RouteHandler }',
    summary: '',
  },
  {
    name: 'createContentNegotiationHandler',
    kind: 'function',
    signature:
      'function createContentNegotiationHandler(opts: ContentNegotiationOptions): Middleware',
    summary: '',
  },
  {
    name: 'createRouteMarkdownResolver',
    kind: 'function',
    signature:
      'function createRouteMarkdownResolver( options: RouteMarkdownResolverOptions, ): MarkdownResolver',
    summary: '',
  },
  {
    name: 'generateA2aCard',
    kind: 'function',
    signature: 'function generateA2aCard(config: A2aCardConfig): A2aCard',
    summary: 'Generate an A2A Agent Card object.',
    agent: true,
  },
  {
    name: 'generateJsonLd',
    kind: 'function',
    signature: 'function generateJsonLd(page: Partial<JsonLdPage>): string',
    summary: 'Generate a JSON-LD object string.',
  },
  {
    name: 'generateLlmsFullTxt',
    kind: 'function',
    signature: 'function generateLlmsFullTxt(config: LlmsTxtConfig): string',
    summary: '',
  },
  {
    name: 'generateLlmsTxt',
    kind: 'function',
    signature: 'function generateLlmsTxt(config: LlmsTxtConfig): string',
    summary: '',
  },
  {
    name: 'generateMcpDiscovery',
    kind: 'function',
    signature: 'function generateMcpDiscovery(config: McpDiscoveryConfig): McpDiscovery',
    summary: 'Generate an MCP Discovery document.',
    agent: true,
  },
  {
    name: 'generateMcpServerCard',
    kind: 'function',
    signature: 'function generateMcpServerCard(config: McpServerCardConfig): McpServerCard',
    summary: '',
  },
  {
    name: 'generateRobotsTxt',
    kind: 'function',
    signature: 'function generateRobotsTxt(config: RobotsConfig = {}): string',
    summary: '',
  },
  {
    name: 'generateSitemapXml',
    kind: 'function',
    signature: 'function generateSitemapXml(config: SitemapConfig): string',
    summary: 'Generate a sitemap.xml string from a list of page URLs.',
  },
  {
    name: 'isAiCrawlerUserAgent',
    kind: 'function',
    signature: 'function isAiCrawlerUserAgent(userAgent: string): boolean',
    summary: 'Does this user-agent belong to an AI crawler?',
    agent: true,
  },
  {
    name: 'readAgentManifestDir',
    kind: 'function',
    signature:
      'async function readAgentManifestDir(dir: string): Promise<ReadonlyArray<AgentMetadata>>',
    summary:
      'Read every `*.agent-manifest.json` in `dir` (non-recursive) and return the components they describe, sorted by tag so the emitted document is byte-stable across runs regardless of directory-read order.',
    agent: true,
  },
  {
    name: 'seoLlmsSections',
    kind: 'function',
    signature:
      'function seoLlmsSections(config: SeoLlmsSectionsConfig): ReadonlyArray<LlmsTxtSection>',
    summary:
      'Returns an array of LlmsTxtSection entries for composition into the `llmsSections` config field — one section titled after the site, linking each sitemap source path.',
  },
  {
    name: 'skillsFromRegistry',
    kind: 'function',
    signature:
      'function skillsFromRegistry( metas: readonly AgentMetadata[] = getAllAgentMetadata(), ): ReadonlyArray<AgentSkill>',
    summary: 'Derive the full set of MCP skills from the live `@aihu/agent` registry.',
    agent: true,
  },
  {
    name: 'viteAgentReadinessIntegration',
    kind: 'function',
    signature:
      'function viteAgentReadinessIntegration( config: AgentReadinessConfig, sources: ViteAgentReadinessSources = {}, ): VitePlugin',
    summary: 'The `viteAgentReadinessIntegration()` Vite plugin (v0.7.4 canonical name).',
  },
  {
    name: 'AGENT_MANIFEST_SUFFIX',
    kind: 'const',
    signature: 'const AGENT_MANIFEST_SUFFIX',
    summary: 'Filename suffix the compiler writes per agent component.',
    agent: true,
  },
  {
    name: 'agentReadiness',
    kind: 'const',
    signature: 'const agentReadiness',
    summary: '',
    deprecated: true,
    agent: true,
  },
  {
    name: 'AI_BOT_LIST',
    kind: 'const',
    signature: 'const AI_BOT_LIST: ReadonlyArray<string>',
    summary: 'All 13 known AI user agents (derived from the registry; order preserved).',
    agent: true,
  },
  {
    name: 'AI_TRAINING_CRAWLER_BOTS',
    kind: 'const',
    signature: 'const AI_TRAINING_CRAWLER_BOTS: ReadonlyArray<string>',
    summary: "Training/scraping crawlers — explicit opt-in; disallowed under `'allow-agents'`.",
    agent: true,
  },
  {
    name: 'AI_USER_FETCHER_BOTS',
    kind: 'const',
    signature: 'const AI_USER_FETCHER_BOTS: ReadonlyArray<string>',
    summary: "User-delegated fetchers / cited-search agents — allowed under `'allow-agents'`.",
    agent: true,
  },
  {
    name: 'BOT_REGISTRY',
    kind: 'const',
    signature: 'const BOT_REGISTRY: ReadonlyArray<BotEntry>',
    summary: 'The ONE unified bot registry: every classified crawler across all three tiers.',
  },
  {
    name: 'SEARCH_BOTS',
    kind: 'const',
    signature: 'const SEARCH_BOTS: ReadonlyArray<string>',
    summary: 'Traditional search-index crawler tokens (derived; `searcher` tier only).',
  },
  {
    name: 'RouteMarkdownResolver',
    kind: 'class',
    signature: 'class RouteMarkdownResolver implements MarkdownResolver',
    summary:
      'The production `MarkdownResolver`: projects route declarations plus the agent-component registry into markdown.',
    agent: true,
  },
  {
    name: 'A2aCapabilities',
    kind: 'interface',
    signature:
      'interface A2aCapabilities {\n  readonly streaming?: boolean\n  readonly pushNotifications?: boolean\n}',
    summary: 'A2A (Agent-to-Agent) Agent Card generator.',
    agent: true,
  },
  {
    name: 'A2aCard',
    kind: 'interface',
    signature:
      'interface A2aCard {\n  readonly name: string\n  readonly description?: string\n  readonly url: string\n  readonly version?: string\n  readonly capabilities: {\n    readonly streaming: boolean\n    readonly pushNotifications: boolean\n  }\n  readonly skills?: ReadonlyArray<A2aSkill>\n}',
    summary: '',
    agent: true,
  },
  {
    name: 'A2aCardConfig',
    kind: 'interface',
    signature:
      'interface A2aCardConfig {\n  readonly name: string\n  readonly description?: string\n  readonly url: string\n  readonly version?: string\n  readonly capabilities?: A2aCapabilities\n  readonly skills?: ReadonlyArray<A2aSkill>\n}',
    summary: '',
    agent: true,
  },
  {
    name: 'A2aSkill',
    kind: 'interface',
    signature:
      'interface A2aSkill {\n  readonly id: string\n  readonly name: string\n  readonly description?: string\n}',
    summary: '',
    agent: true,
  },
  {
    name: 'AgentReadinessSources',
    kind: 'interface',
    signature:
      "interface AgentReadinessSources {\n  /**\n   * Component-metadata reader, mirroring `RouteMarkdownResolverOptions`'\n   * seam of the same name. Defaults to `getAllAgentMetadata` — the live\n   * registry.\n   *\n   * FEL-434b: the registry is EMPTY on a client-target build, because the\n   * compiler elides `registerAgentMetadata` from client JS. Point this at\n   * `readAgentManifestDir(<compiler out dir>)` to build the `## Components`\n   * section from the on-disk agent-meta sidecars instead.\n   */\n  readonly readComponents?: () => ReadonlyArray<AgentMetadata>\n}",
    summary: 'Injectable sources for the generators, alongside the declarative config.',
    agent: true,
  },
  {
    name: 'AgentSkill',
    kind: 'interface',
    signature:
      'interface AgentSkill {\n  readonly id: string\n  readonly name: string\n  readonly description: string\n  readonly inputSchema?: Record<string, unknown>\n}',
    summary: '',
    agent: true,
  },
  {
    name: 'BotEntry',
    kind: 'interface',
    signature:
      'interface BotEntry {\n  readonly agent: string\n  readonly tier: BotTier\n  /** Operator + what the agent actually is — the per-bot classification record. */\n  readonly why: string\n}',
    summary: 'One entry in the classified bot registry.',
  },
  {
    name: 'ComponentMetaLike',
    kind: 'interface',
    signature:
      "interface ComponentMetaLike {\n  readonly tag: string\n  readonly describes?: string\n  readonly state?: Record<string, string>\n  readonly actions?: Record<string, { readonly returns?: Record<string, { type: string }> }>\n  /**\n   * GX Phase 3: the compiled `extract` policy (rides `AgentMetadata`'s open\n   * index signature). Consulted — fail-closed — to decide whether the\n   * component is advertised in llms.txt at all. Absent → advertised (the\n   * resolved default, `read: 'agents'`).\n   */\n  readonly extract?: unknown\n}",
    summary: 'Minimal shape used for the `## Components` section.',
  },
  {
    name: 'ContentNegotiationOptions',
    kind: 'interface',
    signature:
      'interface ContentNegotiationOptions {\n  readonly resolver: MarkdownResolver\n  /**\n   * Token count estimator for the x-markdown-tokens response header.\n   * Default: Math.ceil(content.length / 4).\n   */\n  readonly estimateTokens?: (content: string) => number\n  /**\n   * Serve markdown to a recognized AI crawler that sent no `Accept` preference.\n   * Default: true.\n   *\n   * Rationale: AI crawlers do not send `Accept: text/markdown` — of 7 agents\n   * tested only 3 do, and those are coding agents, not search crawlers\n   * (`docs/domain-hints/seo-and-agent-discoverability.md` §7.5). Without this\n   * the negotiation path is unreachable for the clients with the volume.\n   * Precedent: Next.js 15.2+ keeps an `htmlLimitedBots` list and UA-sniffs it.\n   *\n   * Set false to require an explicit `Accept` header from every client.\n   */\n  readonly userAgentFallback?: boolean\n  /**\n   * Override crawler detection. Defaults to {@link isAiCrawlerUserAgent},\n   * which matches against the shared `AI_BOT_LIST`.\n   */\n  readonly isAgentUserAgent?: (userAgent: string) => boolean\n}',
    summary: '',
  },
  {
    name: 'JsonLdPage',
    kind: 'interface',
    signature:
      "interface JsonLdPage {\n  readonly '@context': string // 'https://schema.org'\n  readonly '@type': string // 'WebPage', 'Article', etc.\n  readonly name?: string\n  readonly description?: string\n  readonly url?: string\n  [key: string]: unknown\n}",
    summary: 'A schema.org page object.',
  },
  {
    name: 'LlmsTxtConfig',
    kind: 'interface',
    signature:
      "interface LlmsTxtConfig {\n  readonly name: string\n  readonly summary?: string\n  readonly sections: ReadonlyArray<LlmsTxtSection>\n  readonly optional?: ReadonlyArray<LlmsTxtLink>\n  /**\n   * Component metadata, rendered as a `## Components` section listing each\n   * component's tag, description, callable actions, and readable state.\n   * When empty (zero components) the section is omitted entirely.\n   *\n   * GX Phase 3 (#437-GX): the section is FILTERED by each component's\n   * compiled `extract` policy — a component whose `read` excludes\n   * user-directed AI fetchers (`'search'`/`'none'`/any hard value) or whose\n   * `call` is closed (`'none'`) is not advertised here (spec §8). Derived,\n   * fail-closed; components without an `extract` member (the pre-GX registry\n   * shape) are advertised exactly as before.\n   */\n  readonly components?: ReadonlyArray<ComponentMetaLike>\n  /**\n   * GX Phase 3 (#437-GX): the compiled route table (structurally,\n   * `@aihu/router` `RouteDefinition[]`). Rendered as a derived `## Routes`\n   * section listing exactly the routes whose declared `read:` admits\n   * user-directed AI fetchers (`'all'`/`'agents'`) — `'search'`/`'none'` and\n   * every hard value are absent from this agent-facing document (spec §8).\n   * One source (the compiled declarations), no hand-maintained route list.\n   * Omitted → no `## Routes` section (byte-identical to pre-GX output).\n   */\n  readonly routes?: ReadonlyArray<RouteReadPolicy>\n  /**\n   * Base URL for derived route links (e.g. `https://example.com`, no trailing\n   * slash). Without it, derived route entries render as bare patterns.\n   */\n  readonly baseUrl?: string\n}",
    summary: '',
  },
  {
    name: 'LlmsTxtLink',
    kind: 'interface',
    signature:
      'interface LlmsTxtLink {\n  readonly title: string\n  readonly url: string\n  readonly description?: string\n}',
    summary: '',
  },
  {
    name: 'LlmsTxtSection',
    kind: 'interface',
    signature:
      'interface LlmsTxtSection {\n  readonly title: string\n  readonly links: ReadonlyArray<LlmsTxtLink>\n}',
    summary: '',
  },
  {
    name: 'MarkdownResolver',
    kind: 'interface',
    signature:
      'interface MarkdownResolver {\n  /**\n   * Return markdown content for the given URL path, or null when none exists.\n   * Implementations must catch errors internally and return null rather than throw.\n   */\n  resolve(path: string): Promise<string | null>\n}',
    summary: 'Abstract interface for resolving markdown content from a URL path.',
  },
  {
    name: 'McpAuthConfig',
    kind: 'interface',
    signature:
      "interface McpAuthConfig {\n  readonly type: 'oauth2'\n  readonly authorizationUrl: string\n  readonly tokenUrl: string\n  readonly scopes?: ReadonlyArray<string>\n  /**\n   * Resource identifier URI (RFC 9728 §2).\n   * Defaults to the `endpoint` URL when not provided.\n   */\n  readonly resourceUri?: string\n}",
    summary: 'OAuth 2.0 auth configuration for a protected MCP endpoint.',
    agent: true,
  },
  {
    name: 'McpDiscovery',
    kind: 'interface',
    signature:
      'interface McpDiscovery {\n  readonly mcpServers: Record<string, McpDiscoveryServer>\n}',
    summary: '',
    agent: true,
  },
  {
    name: 'McpDiscoveryConfig',
    kind: 'interface',
    signature:
      'interface McpDiscoveryConfig {\n  readonly name: string\n  readonly url: string\n  readonly description?: string\n}',
    summary: '',
    agent: true,
  },
  {
    name: 'McpDiscoveryServer',
    kind: 'interface',
    signature:
      'interface McpDiscoveryServer {\n  readonly url: string\n  readonly name: string\n  readonly description?: string\n}',
    summary: 'MCP Discovery document generator.',
    agent: true,
  },
  {
    name: 'McpServerCard',
    kind: 'interface',
    signature:
      "interface McpServerCard {\n  readonly $schema: 'https://modelcontextprotocol.io/schemas/server-card/v1.0'\n  readonly version: '1.0'\n  readonly protocolVersion: string\n  readonly serverInfo: {\n    readonly name: string\n    readonly version: string\n    readonly description?: string\n    readonly homepage?: string\n  }\n  readonly transport: {\n    readonly type: 'streamable-http' | 'sse'\n    readonly url: string\n  }\n  readonly capabilities: {\n    readonly tools: boolean\n    readonly resources: boolean\n    readonly prompts: boolean\n  }\n  readonly tools?: ReadonlyArray<{\n    readonly name: string\n    readonly description: string\n  }>\n  readonly auth?: {\n    readonly type: 'oauth2'\n    /**\n     * OAuth 2.0 authorization-server issuer identifier (the origin of the\n     * configured token endpoint). A consumer performs RFC 8414 metadata\n     * discovery against the issuer itself — we do NOT synthesize or advertise a\n     * `/.well-known/oauth-authorization-server` URL here: that document belongs\n     * on the authorization server, not on this MCP server, and we don't serve it.\n     */\n    readonly authorizationServer: string\n  }\n}",
    summary: 'MCP Server Card output object.',
    agent: true,
  },
  {
    name: 'McpServerCardConfig',
    kind: 'interface',
    signature:
      "interface McpServerCardConfig {\n  readonly name: string\n  readonly version: string\n  readonly endpoint: string\n  readonly skills?: ReadonlyArray<AgentSkill>\n  /**\n   * Component metadata to derive skills from. Defaults to the live\n   * `@aihu/agent` registry.\n   *\n   * FEL-434b: on a client-target build the registry is empty (the compiler\n   * elides `registerAgentMetadata` from client JS), so the caller passes the\n   * components read back from the on-disk agent-meta sidecars instead. Same\n   * `extract` filtering applies either way — see `skillsFromRegistry`.\n   */\n  readonly components?: ReadonlyArray<AgentMetadata>\n  readonly auth?: McpAuthConfig\n  readonly description?: string\n  readonly homepage?: string\n  /** Default: '2025-06-18'. */\n  readonly protocolVersion?: string\n  /** Default: 'streamable-http'. */\n  readonly transportType?: 'streamable-http' | 'sse'\n}",
    summary: '',
    agent: true,
  },
  {
    name: 'RobotsConfig',
    kind: 'interface',
    signature:
      "interface RobotsConfig {\n  /**\n   * AI-agent crawl policy. Default: `'allow-agents'` (tiered) — user-delegated\n   * fetchers get `Allow: /`, training/scraping crawlers get `Disallow: /`.\n   * `'allow-all'` and `'deny-all'` apply one directive to all 13 bots.\n   * An array of rules gives per-bot control.\n   */\n  readonly aiAgents?: AiAgentsPolicy | ReadonlyArray<RobotsRule>\n  readonly standard?: ReadonlyArray<RobotsRule>\n  /**\n   * GX Phase 3 (#437-GX): the compiled route table (structurally, `@aihu/router`\n   * `RouteDefinition[]` — e.g. the `virtual:aihu-routes` module). Each route's\n   * compiled `extract.read` declaration derives per-path directives over the\n   * bot-registry tiers (see {@link deriveRouteDirectives} for the mapping).\n   * Omitted, or with every route at the recorded default (`read: 'agents'`),\n   * output is byte-identical to the #430 tiered default. COMPLIANCE-TIER:\n   * advisory for compliant crawlers; the origin gate is the authority.\n   */\n  readonly routes?: ReadonlyArray<RouteReadPolicy>\n  readonly sitemap?: string\n  /**\n   * The trailing `User-agent: * / Allow: /` block is ALWAYS emitted for\n   * predictable output — set `wildcard: false` to suppress it. It is also\n   * skipped automatically when one of your own rules already targets `*`\n   * (your rule IS the wildcard decision; emitting a second block would\n   * contradict it).\n   */\n  readonly wildcard?: boolean\n}",
    summary: '',
  },
  {
    name: 'RobotsRule',
    kind: 'interface',
    signature:
      'interface RobotsRule {\n  readonly userAgent: string | ReadonlyArray<string>\n  readonly allow?: ReadonlyArray<string>\n  readonly disallow?: ReadonlyArray<string>\n  readonly crawlDelay?: number\n}',
    summary: '',
  },
  {
    name: 'RouteMarkdownEntry',
    kind: 'interface',
    signature:
      "interface RouteMarkdownEntry {\n  /** URL path this entry answers for, e.g. `/about`. Leading slash required. */\n  readonly path: string\n  /** Derived from the route's `@route { head }` title. */\n  readonly title?: string\n  /** Derived from the route's `@route { head }` description/meta. */\n  readonly description?: string\n  /** Primary prose content for the route, already markdown. */\n  readonly body?: string\n  /**\n   * Tags of the agent-annotated components mounted on this route. Each is\n   * expanded from the live registry into its callable actions and readable\n   * state — a surface that NEVER appears in the rendered HTML, and therefore\n   * cannot be recovered by any HTML→markdown converter.\n   */\n  readonly components?: ReadonlyArray<string>\n}",
    summary:
      "A route's agent-facing declaration: the same source that builds the UI, projected into the shape an agent asked for.",
    agent: true,
  },
  {
    name: 'RouteMarkdownResolverOptions',
    kind: 'interface',
    signature:
      'interface RouteMarkdownResolverOptions {\n  readonly routes: ReadonlyArray<RouteMarkdownEntry>\n  /** Site name, used as the document title when a route declares none. */\n  readonly siteName?: string\n  /**\n   * Component-registry reader. Defaults to `getAllAgentMetadata` from\n   * `@aihu/agent`. Injectable so the resolver stays testable without a\n   * populated global registry.\n   */\n  readonly readComponents?: () => ReadonlyArray<ComponentMetaLike>\n}',
    summary: '',
  },
  {
    name: 'RouteReadPolicy',
    kind: 'interface',
    signature:
      'interface RouteReadPolicy {\n  /** Route pattern, e.g. `/pricing` or `/reports/:id`. */\n  readonly pattern: string\n  /** The compiled `extract` object; only `read` is consulted here. */\n  readonly extract?: { readonly read?: unknown } | undefined\n}',
    summary:
      "One compiled route's crawl policy — the `robots` projection of a `RouteDefinition` (`@aihu/router`) or `.route.json` sidecar.",
  },
  {
    name: 'SeoLlmsSectionsConfig',
    kind: 'interface',
    signature:
      "interface SeoLlmsSectionsConfig {\n  readonly siteName: string\n  readonly baseUrl: string // e.g. 'https://example.com' (no trailing slash)\n  readonly sitemapSources?: ReadonlyArray<{ readonly path: string }>\n}",
    summary: '',
  },
  {
    name: 'SitemapConfig',
    kind: 'interface',
    signature: 'interface SitemapConfig {\n  readonly pages: ReadonlyArray<SitemapUrl>\n}',
    summary: '',
  },
  {
    name: 'SitemapUrl',
    kind: 'interface',
    signature:
      'interface SitemapUrl {\n  readonly url: string\n  readonly lastmod?: string\n  readonly changefreq?: SitemapChangefreq\n  readonly priority?: number\n}',
    summary: '',
  },
  {
    name: 'ViteAgentReadinessSources',
    kind: 'interface',
    signature:
      "interface ViteAgentReadinessSources extends AgentReadinessSources {\n  /**\n   * Directory the compiler wrote its `<tag>.agent-manifest.json` sidecars to\n   * (the `--out` dir). When set, the `## Components` section and the MCP\n   * server card's skills are derived from those files instead of the live\n   * registry — the fix for a client-target build listing nothing (FEL-434b).\n   * Overrides `readComponents`. A missing directory reads as zero components.\n   */\n  readonly agentManifestDir?: string\n}",
    summary: '`AgentReadinessSources` plus the build-time-only sidecar directory.',
    agent: true,
  },
  {
    name: 'AgentReadinessConfig',
    kind: 'type',
    signature: 'type AgentReadinessConfig',
    summary: '',
    agent: true,
  },
  {
    name: 'BotTier',
    kind: 'type',
    signature: "type BotTier = 'searcher' | 'user-fetcher' | 'training-crawler'",
    summary: 'Crawler tier — the classification the GX `read:` axis consumes.',
  },
  {
    name: 'SitemapChangefreq',
    kind: 'type',
    signature:
      "type SitemapChangefreq =\n  | 'always'\n  | 'hourly'\n  | 'daily'\n  | 'weekly'\n  | 'monthly'\n  | 'yearly'\n  | 'never'",
    summary: 'XML Sitemap generator.',
  },
]
