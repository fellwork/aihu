// GENERATED by scripts/gen-api.ts — do not edit. Source: packages/store
import type { ApiExport, ApiPackage } from '../types.ts'

export const PKG: ApiPackage = {
  name: '@aihu/store',
  slug: 'store',
  tier: 'Runtime core',
  version: '0.1.0',
  tagline:
    'Pinia-style global stores on aihu signals — defineStore, SSR-safe per-request instances, registry-based serialize/hydrate, plugins.',
  note: '',
}

export const EXPORTS: readonly ApiExport[] = [
  {
    name: '_resetStoreRegistry',
    kind: 'function',
    signature: 'function _resetStoreRegistry(): void',
    summary: '',
  },
  {
    name: 'createPersistPlugin',
    kind: 'function',
    signature: 'function createPersistPlugin(config: PersistPluginConfig = {}): StorePlugin',
    summary: 'Build a persistence plugin.',
  },
  {
    name: 'defineStore',
    kind: 'function',
    signature:
      'function defineStore( id: string, setupOrConfig: | (() => Record<string, unknown>) | OptionsStoreConfig<StateTree, GettersTree<StateTree>, ActionsTree>, options?: StoreCustomOptions, ): UseStore<AnyStore>',
    summary: '',
  },
  {
    name: 'hydrateStores',
    kind: 'function',
    signature: 'function hydrateStores(snapshot: Record<string, StateTree>): void',
    summary: 'Pre-seed store state from a server snapshot.',
  },
  {
    name: 'registerStorePlugin',
    kind: 'function',
    signature: 'function registerStorePlugin(plugin: StorePlugin): Dispose',
    summary: 'Register a plugin globally.',
  },
  {
    name: 'serializeStores',
    kind: 'function',
    signature: 'function serializeStores(): Record<string, StateTree>',
    summary:
      'Snapshot every instantiated store in the ACTIVE registry (per-request on the server) as `{ storeId: { stateKey: value } }`.',
  },
  {
    name: 'persistPlugin',
    kind: 'const',
    signature: 'const persistPlugin: StorePlugin',
    summary: 'Ready-made persistence plugin with default config (localStorage, `aihu:`).',
  },
  {
    name: 'ActionContext',
    kind: 'interface',
    signature:
      "interface ActionContext<S> {\n  /** The store instance the action belongs to. */\n  store: S\n  /** The action's key on the store. */\n  name: string\n  /** The arguments the action was called with. */\n  args: unknown[]\n  /**\n   * Register a hook that runs after the action returns (or, for an\n   * async action, after the returned promise resolves).\n   */\n  after(cb: (result: unknown) => void): void\n  /**\n   * Register a hook that runs when the action throws (or the returned\n   * promise rejects). The error is always re-thrown to the caller.\n   */\n  onError(cb: (error: unknown) => void): void\n}",
    summary:
      'Context handed to every `$onAction` subscriber at the moment an action is invoked (before the action body runs).',
  },
  {
    name: 'OptionsStoreConfig',
    kind: 'interface',
    signature:
      'interface OptionsStoreConfig<\n  S extends StateTree,\n  G extends GettersTree<S>,\n  A extends ActionsTree,\n> {\n  state: () => S\n  getters?: G\n  actions?: A & ThisType<OptionsStore<S, G, A>>\n}',
    summary: 'Options-style store definition.',
  },
  {
    name: 'PersistPluginConfig',
    kind: 'interface',
    signature:
      'interface PersistPluginConfig {\n  /** Storage backend; defaults to `window.localStorage` (client only). */\n  storage?: StorageLike\n  /** Key prefix, defaults to `"aihu:"`. Full key: `${prefix}${key ?? id}`. */\n  prefix?: string\n}',
    summary: '',
  },
  {
    name: 'PersistStoreOptions',
    kind: 'interface',
    signature: 'interface PersistStoreOptions {\n  key?: string\n}',
    summary: 'Per-store opt-in shape: `{ persist: true }` or `{ persist: { key } }`.',
  },
  {
    name: 'StorageLike',
    kind: 'interface',
    signature:
      'interface StorageLike {\n  getItem(key: string): string | null\n  setItem(key: string, value: string): void\n}',
    summary: 'Minimal storage contract (satisfied by `window.localStorage`).',
  },
  {
    name: 'StoreBase',
    kind: 'interface',
    signature:
      "interface StoreBase<St extends StateTree = StateTree> {\n  /** The store's unique id (registry key + serialization key). */\n  readonly $id: string\n  /**\n   * Apply several state writes as one batch. Object form writes the given\n   * state keys; function form receives the store and may call any setters —\n   * either way subscribers are notified once.\n   */\n  $patch(patch: Partial<St> | ((store: this) => void)): void\n  /**\n   * Reset state back to the `state()` factory's initial values.\n   * Options-style stores only: a setup store has no canonical initial-state\n   * factory to re-run (its initial values are arbitrary expressions inside\n   * the setup closure), so `$reset()` throws for setup-style stores.\n   */\n  $reset(): void\n  /**\n   * Subscribe to state changes. `cb` receives a fresh snapshot of the\n   * store's state after each change (batched writes notify once).\n   * Returns a dispose function.\n   */\n  $subscribe(cb: (state: St) => void): Dispose\n  /** Hook before/after/onError around every action call. Returns dispose. */\n  $onAction(cb: ActionSubscriber<this>): Dispose\n  /**\n   * Tear the store down: dispose subscriptions and store-owned computeds,\n   * then remove the instance from the registry (the next `useStore()`\n   * call re-instantiates fresh).\n   */\n  $dispose(): void\n}",
    summary:
      'The `$`-prefixed instance surface every store carries, regardless of definition style.',
    agent: true,
  },
  {
    name: 'StorePluginContext',
    kind: 'interface',
    signature:
      "interface StorePluginContext {\n  /** The just-built store instance (after hydration adoption). */\n  store: AnyStore\n  /** The store id. */\n  id: string\n  /** The per-store custom options bag (`defineStore`'s third argument). */\n  options: StoreCustomOptions | undefined\n}",
    summary: 'Context handed to every registered plugin at store instantiation.',
  },
  {
    name: 'ActionsTree',
    kind: 'type',
    signature: 'type ActionsTree = Record<string, (...args: never[]) => unknown>',
    summary: '',
  },
  {
    name: 'ActionSubscriber',
    kind: 'type',
    signature: 'type ActionSubscriber<S> = (context: ActionContext<S>) => void',
    summary: '',
  },
  {
    name: 'AnyStore',
    kind: 'type',
    signature: 'type AnyStore = StoreBase & Record<string, unknown>',
    summary: 'Widest store instance type — used by the registry, plugins, and SSR.',
  },
  {
    name: 'GetterReads',
    kind: 'type',
    signature:
      'type GetterReads<S extends StateTree, G extends GettersTree<S>> = {\n  readonly [K in keyof G]: G[K] extends (state: StateReads<S>) => infer R ? Read<R> : never\n}',
    summary: 'Each getter lowers onto the instance as a computed read function.',
  },
  {
    name: 'GettersTree',
    kind: 'type',
    signature:
      'type GettersTree<S extends StateTree> = Record<string, (state: StateReads<S>) => unknown>',
    summary: 'Getters receive the read-side state view and return a derived value.',
  },
  {
    name: 'OptionsStore',
    kind: 'type',
    signature:
      'type OptionsStore<\n  S extends StateTree,\n  G extends GettersTree<S>,\n  A extends ActionsTree,\n> = StateReads<S> & StateWrites<S> & GetterReads<S, G> & A & StoreBase<S>',
    summary: 'Full instance type of an options-style store.',
  },
  {
    name: 'SetupSnapshot',
    kind: 'type',
    signature:
      'type SetupSnapshot<SS> = {\n  [K in SetupStateKeys<SS>]: SS[K] extends Read<infer T> ? T : never\n}',
    summary: 'The serializable snapshot shape of a setup-style store.',
  },
  {
    name: 'SetupStateKeys',
    kind: 'type',
    signature:
      'type SetupStateKeys<SS> = {\n  [K in keyof SS & string]: SS[K] extends Read<unknown>\n    ? `set${Capitalize<K>}` extends keyof SS\n      ? K\n      : never\n    : never\n}[keyof SS & string]',
    summary:
      'State detection for setup-style stores — mirrored at runtime in `store.ts` (`collectSetupShape`): a key `k` is a state entry when the setup returns a zero-arg read function under `k` AND a setter under `set${Capitalize<k>}`.',
  },
  {
    name: 'SetupStore',
    kind: 'type',
    signature:
      'type SetupStore<SS extends Record<string, unknown>> = SS & StoreBase<SetupSnapshot<SS>>',
    summary: 'Full instance type of a setup-style store.',
  },
  {
    name: 'StateReads',
    kind: 'type',
    signature: 'type StateReads<S extends StateTree> = { readonly [K in keyof S]: Read<S[K]> }',
    summary: "Read-side view of an options-style store's state.",
  },
  {
    name: 'StateTree',
    kind: 'type',
    signature: 'type StateTree = Record<string, unknown>',
    summary: 'A plain-object state tree: what a store serializes to / hydrates from.',
  },
  {
    name: 'StateWrites',
    kind: 'type',
    signature:
      'type StateWrites<S extends StateTree> = {\n  readonly [K in keyof S & string as `set${Capitalize<K>}`]: Write<S[K]>\n}',
    summary: 'Write-side view: each state key `k` lowers to a `setK` writer.',
  },
  {
    name: 'StoreCustomOptions',
    kind: 'type',
    signature: 'type StoreCustomOptions = Record<string, unknown>',
    summary: 'Per-store options bag consumed by plugins (e.g.',
  },
  {
    name: 'StorePlugin',
    kind: 'type',
    signature:
      'type StorePlugin = (context: StorePluginContext) => Record<string, unknown> | undefined',
    summary: 'A store plugin.',
  },
  {
    name: 'UseStore',
    kind: 'type',
    signature: 'type UseStore<T> = (() => T) & { readonly $id: string }',
    summary: 'What `defineStore` returns: a lazy accessor for the store instance.',
  },
]
