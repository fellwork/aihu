@route {
  path: "/guides/ssr-hydration",
  name: "guide-ssr-hydration",
  layout: "docs",
  head: {
    title: "SSR & Hydration — aihu",
    description: "Build targets, the @route ssr flag, the fetch-API server router, renderToStream/renderToString, and hydrate() vs mount().",
    canonical: "/guides/ssr-hydration"
  }
}

@state {
  import { renderMarkdown } from '../../lib/markdown.ts'
  import { SSR_HYDRATION } from '../../data/guide-ssr-hydration.ts'

  const body = renderMarkdown(SSR_HYDRATION)
}

@template {
  <div class="dn-guide">
    <p class="dn-sec-num"><b>G · 06</b> guides</p>
    <p class="dn-breadcrumb dn-rail"><a href="/guides/getting-started">Guides</a> / SSR &amp; Hydration</p>
    <article class="dn-prose dn-article" html={body}></article>
    <nav class="dn-page-nav" aria-label="Guide pagination">
      <a class="dn-page-prev" href="/guides/routing-layouts">
        <span>Previous</span>
        <strong>← Routing &amp; Layouts</strong>
      </a>
      <a class="dn-page-next" href="/guides/agent-discovery">
        <span>Next</span>
        <strong>Agent Discovery →</strong>
      </a>
    </nav>
  </div>
}

@style {
  .dn-guide {
    max-width: var(--content-max);
  }
  .dn-guide > .dn-sec-num {
    margin: 0 0 1rem;
  }
  .dn-breadcrumb {
    margin: 0 0 2rem;
  }
  .dn-breadcrumb a {
    color: var(--muted);
  }
  .dn-page-nav {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    margin-top: 3.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--border);
  }
  .dn-page-prev,
  .dn-page-next {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
    padding: 0.85rem 1.2rem;
    border: 1px solid var(--border);
    border-radius: var(--r-md);
    text-decoration: none;
    color: var(--fg);
  }
  .dn-page-next {
    align-items: flex-end;
  }
  .dn-page-prev:hover,
  .dn-page-next:hover {
    text-decoration: none;
    border-color: var(--accent-line);
  }
  .dn-page-prev span,
  .dn-page-next span {
    font-size: var(--fs-xs);
    color: var(--faint);
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }
  .dn-page-prev strong,
  .dn-page-next strong {
    color: var(--accent);
    font-weight: 600;
  }
}
