# @aihu/ai

Thin adapters from AI SDK stream types to `ReadableStream<string>` for aihu `$stream` collections.

> **Status: stub.** A full guide is coming in the docs-content refresh. Until then, the package [README](https://github.com/fellwork/aihu/tree/main/packages/ai) is the authoritative reference.

## Install

```bash
bun add @aihu/ai
# or
npm install @aihu/ai
```

## Full guide coming

This page is a placeholder so `@aihu/ai` has doc coverage. The complete guide — adapting AI SDK stream types into the `ReadableStream<string>` shape aihu `$stream` collections consume — lands in the docs-content refresh. For now, see the [package README](https://github.com/fellwork/aihu/tree/main/packages/ai).
