# @aihu/store

Pinia-style global stores on aihu signals — `defineStore`, SSR-safe per-request instances, registry-based serialize/hydrate, and store plugins.

> **Status: stub.** A full guide is coming in the docs-content refresh. Until then, the package [README](https://github.com/fellwork/aihu/tree/main/packages/store) is the authoritative reference.

## Install

```bash
bun add @aihu/store
# or
npm install @aihu/store
```

## Full guide coming

This page is a placeholder so `@aihu/store` has doc coverage. The complete guide — `defineStore`, per-request SSR store instances, registry-based serialize/hydrate, and the store-plugin surface — lands in the docs-content refresh. For now, see the [package README](https://github.com/fellwork/aihu/tree/main/packages/store).
