# @aihu/mcp

MCP server for aihu — exposes the `aihu_example` (cookbook retrieval) and `aihu_validate` tools over the stdio transport.

> **Status: stub.** A full guide is coming in the docs-content refresh. Until then, the package [README](https://github.com/fellwork/aihu/tree/main/packages/mcp) is the authoritative reference.

## Install

```bash
bun add @aihu/mcp
# or
npm install @aihu/mcp
```

## Full guide coming

This page is a placeholder so `@aihu/mcp` has doc coverage. The complete guide — wiring the stdio MCP server, the `aihu_example` cookbook-retrieval tool, and `aihu_validate` — lands in the docs-content refresh. For now, see the [package README](https://github.com/fellwork/aihu/tree/main/packages/mcp).
