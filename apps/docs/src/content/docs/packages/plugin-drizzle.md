# @aihu-plugin/drizzle

Drizzle ORM data adapter for aihu — typed `createResource` fetchers and `defineLoader` helpers (Postgres / SQLite / libSQL).

> **Status: stub.** A full guide is coming in the docs-content refresh. Until then, the package [README](https://github.com/fellwork/aihu/tree/main/packages/plugin-drizzle) is the authoritative reference.

## Install

```bash
bun add @aihu-plugin/drizzle
# or
npm install @aihu-plugin/drizzle
```

## Full guide coming

This page is a placeholder so `@aihu-plugin/drizzle` has doc coverage. The complete guide — typed Drizzle-backed `createResource` fetchers and `defineLoader` helpers across Postgres / SQLite / libSQL — lands in the docs-content refresh. For now, see the [package README](https://github.com/fellwork/aihu/tree/main/packages/plugin-drizzle).
