{
  "Contract block scaffold": {
    "prefix": "contract-block",
    "scope": "scribe",
    "body": [
      "<contract>",
      "input ${1:name}: ${2|string,number,boolean|} = ${3:default}",
      "state ${4:internal}: ${5|string,number,boolean|}",
      "action ${6:run}() -> { ${7:result}: ${8|string,number,boolean|} }",
      "</contract>$0"
    ],
    "description": "Insert a full <contract> block with one input, one state, and one action."
  },
  "Contract input declaration": {
    "prefix": "contract-input",
    "scope": "scribe",
    "body": [
      "input ${1:name}: ${2|string,number,boolean|} = ${3:default}$0"
    ],
    "description": "Insert a single input declaration with a default value."
  },
  "Contract input (enum)": {
    "prefix": "contract-input-enum",
    "scope": "scribe",
    "body": [
      "input ${1:name}: enum(${2:variant_a}, ${3:variant_b}, ${4:variant_c}) = ${2:variant_a}$0"
    ],
    "description": "Insert an enum input with a default variant."
  },
  "Contract action with returns": {
    "prefix": "contract-action",
    "scope": "scribe",
    "body": [
      "action ${1:run}() -> { ${2:field}: ${3|string,number,boolean|} }$0"
    ],
    "description": "Insert an action declaration with a single-field returns block."
  },
  "Contract state declaration": {
    "prefix": "contract-state",
    "scope": "scribe",
    "body": [
      "state ${1:name}: ${2|string,number,boolean|}$0"
    ],
    "description": "Insert a state declaration (internal-only; not in MCP manifest)."
  },
  "Script setup block": {
    "prefix": "script-setup",
    "scope": "scribe",
    "body": [
      "<script setup lang=\"ts\" name=\"${1:my-component}\">",
      "import { signal, computed } from '@scribe/signals'",
      "",
      "$0",
      "</script>"
    ],
    "description": "Insert a <script setup lang=\"ts\"> block with the standard signals import."
  }
}
